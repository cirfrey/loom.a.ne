#include "lm/config.hpp"

namespace lm
{
    // Yes, this is really all there is to it.
    // If you were expecting more you should probably
    // look into lm::hook::*config.
    config_t config;
}
